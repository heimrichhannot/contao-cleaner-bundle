services:
  _defaults:
    autowire: true
    autoconfigure: true
    bind:
      $projectDir: '%kernel.project_dir%'

  HeimrichHannot\CleanerBundle\:
    resource: '../src/{Command,Cron,DataContainer,Registry}'
